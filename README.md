Build with `mvn clean package` and install by clicking "Upload add-on" in the "Manage add-ons" section in Confluence Administration.

After the Webhook URL has been setup in the "BearyChat Robots", you can configure notification channels in the Space Administration.